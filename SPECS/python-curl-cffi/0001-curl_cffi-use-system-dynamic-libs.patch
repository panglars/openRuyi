diff -Naur a/libs.json b/libs.json
--- a/libs.json	2026-01-31 13:28:42
+++ b/libs.json	2026-01-31 13:28:50
@@ -48,88 +48,88 @@
         "system": "Linux",
         "machine": "x86_64",
         "pointer_size": 64,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "gnu",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "x86_64"
     },
     {
         "system": "Linux",
         "machine": "x86_64",
         "pointer_size": 64,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "musl",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "x86_64"
     },
     {
         "system": "Linux",
         "machine": "i686",
         "pointer_size": 32,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "gnu",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "i386"
     },
     {
         "system": "Linux",
         "machine": "aarch64",
         "pointer_size": 64,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "gnu",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "aarch64"
     },
     {
         "system": "Linux",
         "machine": "riscv64",
         "pointer_size": 64,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "gnu",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "riscv64"
     },
     {
         "system": "Linux",
         "machine": "aarch64",
         "pointer_size": 64,
-        "libdir": "~/.local/lib",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
         "link_type": "dynamic",
         "libc": "musl",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "aarch64"
     },
     {
         "system": "Linux",
         "machine": "armv6l",
         "pointer_size": 32,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "gnueabihf",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "arm"
     },
     {
         "system": "Linux",
         "machine": "armv7l",
         "pointer_size": 32,
-        "libdir": "",
+        "libdir": "/usr/lib64",
         "sysname": "linux",
-        "link_type": "static",
+        "link_type": "dynamic",
         "libc": "gnueabihf",
-        "so_name": "libcurl-impersonate.so",
+        "so_name": "libcurl-impersonate-chrome.so",
         "so_arch": "arm"
     }
 ]
diff -Naur a/scripts/build.py b/scripts/build.py
--- a/scripts/build.py	2026-01-31 13:28:42
+++ b/scripts/build.py	2026-01-31 13:29:01
@@ -34,11 +34,13 @@
             and ("libc" not in arch or arch.get("libc") == libc)
         ):
             if arch["libdir"]:
-                arch["libdir"] = os.path.expanduser(arch["libdir"])
+                # Use system libdir as-is for absolute paths
+                if not arch["libdir"].startswith("/"):
+                    arch["libdir"] = os.path.expanduser(arch["libdir"])
             else:
                 global tmpdir
                 if "CI" in os.environ:
-                    tmpdir = "./tmplibdir"
+                    tmpdir = os.path.expanduser("~/tmp/curl_cffi_build")
                     os.makedirs(tmpdir, exist_ok=True)
                     arch["libdir"] = tmpdir
                 else:
@@ -53,6 +55,11 @@
 
 
 def download_libcurl():
+    # If using system libraries (dynamic linking with absolute path), skip download
+    if arch.get("link_type") == "dynamic" and arch["libdir"].startswith("/usr"):
+        print(f"Using system library from {arch['libdir']}")
+        return
+
     if (Path(arch["libdir"]) / arch["so_name"]).exists():
         print(".so files already downloaded.")
         return
@@ -133,7 +140,7 @@
     elif arch["system"] == "Darwin" or (
         arch["system"] == "Linux" and arch.get("link_type") == "dynamic"
     ):
-        return ["curl-impersonate"]
+        return ["curl-impersonate-chrome"]
     else:
         return []
 
