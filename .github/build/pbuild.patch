This patch was generated by LLM; please do not upload it upstream.

diff --git a/PBuild/Expand.pm b/PBuild/Expand.pm
index 0a00e3c..239b1c0 100644
--- a/PBuild/Expand.pm
+++ b/PBuild/Expand.pm
@@ -127,7 +127,7 @@ sub expand_deps {
     return;
   }
   if ($p->{'genbuildreqs'}) {
-    push @deps, @{$p->{'genbuildreqs'}};
+    push @deps, @{$p->{'genbuildreqs'}->[1]};
   }
   my @edeps;
   if ($cross) {
diff --git a/PBuild/Job.pm b/PBuild/Job.pm
index 70db559..58f77e9 100644
--- a/PBuild/Job.pm
+++ b/PBuild/Job.pm
@@ -22,6 +22,7 @@ package PBuild::Job;
 
 use strict;
 
+use Digest::MD5 ();
 use Time::HiRes ();
 
 use PBuild::Util;
@@ -455,7 +456,15 @@ sub finishjob {
   }
 
   if ($ret == 9) {
-    die("XXX: dynamic buildreqs not implemented yet");
+    my $buildreqsfile = "$buildroot/.build.packages/OTHER/_generated_buildreqs";
+    my $filecontent = PBuild::Util::readstr($buildreqsfile, 1);
+    if (defined($filecontent) && $filecontent ne '') {
+      my $result = { '_log' => "$buildroot/.build.log", '_generated_buildreqs' => $filecontent };
+      return ('genbuildreqs', $result);
+    }
+    # file missing or empty, treat as failure
+    my $result = { '_log' => "$buildroot/.build.log" };
+    return ('failed', $result);
   }
   if ($ret) {
     my $result = { '_log' => "$buildroot/.build.log" };
@@ -466,4 +475,60 @@ sub finishjob {
   return ('succeeded', $result);
 }
 
+#
+# Update and persist generated build requirements for a package.
+# Stores per-package at $builddir/$packid/_genbuildreqs.
+# Returns 1 if genbuildreqs changed, 0 if unchanged.
+#
+sub set_genbuildreqs {
+  my ($builddir, $pkgsrc, $packid, $filecontent) = @_;
+  my $p = $pkgsrc->{$packid};
+  my $verifymd5 = $p->{'verifymd5'} || $p->{'srcmd5'};
+  my $dst = "$builddir/$packid";
+
+  if (defined($filecontent) && $filecontent ne '') {
+    my $md5 = Digest::MD5::md5_hex($filecontent);
+    # check if unchanged
+    my $old = PBuild::Util::retrieve("$dst/_genbuildreqs", 1);
+    if ($old && $old->[0] eq $md5 && ($old->[2] || '') eq ($verifymd5 || '')) {
+      return 0;
+    }
+    my $genbuildreqs = [ $md5, [ split("\n", $filecontent) ], $verifymd5 ];
+    PBuild::Util::mkdir_p($dst);
+    PBuild::Util::store("$dst/._genbuildreqs.$$", "$dst/_genbuildreqs", $genbuildreqs);
+    $p->{'genbuildreqs'} = $genbuildreqs;
+  } else {
+    return 0 unless -e "$dst/_genbuildreqs";
+    unlink("$dst/_genbuildreqs");
+    delete $p->{'genbuildreqs'};
+  }
+  return 1;
+}
+
+#
+# Load persisted genbuildreqs from disk and apply to pkgsrc.
+# Reads per-package from $builddir/$packid/_genbuildreqs.
+# Invalidates entries whose source (verifymd5) has changed.
+#
+sub load_genbuildreqs {
+  my ($builddir, $pkgsrc) = @_;
+  for my $packid (sort keys %$pkgsrc) {
+    my $dst = "$builddir/$packid";
+    next unless -e "$dst/_genbuildreqs";
+    my $genbuildreqs = PBuild::Util::retrieve("$dst/_genbuildreqs", 1);
+    if (!$genbuildreqs) {
+      unlink("$dst/_genbuildreqs");
+      next;
+    }
+    my $p = $pkgsrc->{$packid};
+    my $verifymd5 = $p->{'verifymd5'} || $p->{'srcmd5'};
+    # invalidate if source changed
+    if ($genbuildreqs->[2] && $genbuildreqs->[2] ne ($verifymd5 || '')) {
+      unlink("$dst/_genbuildreqs");
+      next;
+    }
+    $p->{'genbuildreqs'} = $genbuildreqs;
+  }
+}
+
 1;
diff --git a/pbuild b/pbuild
index 6697d0f..8a4f333 100755
--- a/pbuild
+++ b/pbuild
@@ -479,6 +479,9 @@ if ($cross) {
   }
 }
 
+# load persisted genbuildreqs and apply to packages
+PBuild::Job::load_genbuildreqs($builddir, \%pkgsrc);
+
 # setup builders
 my @builders;
 for my $no (1..$opts->{'buildjobs'}) {
@@ -535,7 +538,12 @@ while (1) {
     if ($code eq 'building') {
       my @building = map {$_->{'job'}} grep {$_->{'job'}} @builders;
       my $job = PBuild::Job::waitjob($opts, @building);
-      ($code) = PBuild::Job::finishjob($job);
+      my $buildresult;
+      ($code, $buildresult) = PBuild::Job::finishjob($job);
+      if ($code eq 'genbuildreqs') {
+        PBuild::Job::set_genbuildreqs($builddir, \%pkgsrc, $pkg, $buildresult->{'_generated_buildreqs'});
+        $code = 'failed';	# cannot retry in extra-packs mode
+      }
     } else {
       $details = ": $result->{$pkg}->{'details'}" if $result->{$pkg}->{'details'};
     }
@@ -585,6 +593,17 @@ while (1) {
   my $bid = ($job->{'nbuilders'} || 1) > 1 ? "$job->{'name'}: " : '';
   print "${bid}finished $p->{'pkg'}/$p->{'recipe'} after ${duration}: $code\n";
 
+  # handle dynamic build requirements
+  if ($code eq 'genbuildreqs') {
+    my $packid = $p->{'pkg'};
+    my $changed = PBuild::Job::set_genbuildreqs($builddir, \%pkgsrc, $packid, $buildresult->{'_generated_buildreqs'});
+    if ($changed) {
+      print "${bid}$packid: build requires changed, scheduling rebuild\n";
+      undef $ctx;
+    }
+    next;
+  }
+
   my $jobhist = PBuild::BuildResult::makejobhist($p, $code, $job->{'readytime'}, $job->{'starttime'}, $job->{'endtime'}, $job->{'reason'}, $job->{'hostarch'});
   PBuild::BuildResult::addjobhist($builddir, $jobhist);
 
